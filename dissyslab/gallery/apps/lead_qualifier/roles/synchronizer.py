"""Per-office synchronizer.

Auto-generated by
`dissyslab.patterns.sense_think_respond.build_office`.

Waits for one message on each of 3 named inports — summarizer, sentiment_classifier, urgency_classifier — dict-merges the messages,
and emits the merged article on `out`.
"""
from dissyslab.core import Agent
from dissyslab.office_v2 import AgentRoleEntry


class _Synchronizer(Agent):
    def __init__(self, name=None):
        super().__init__(
            name=name,
            inports=['summarizer', 'sentiment_classifier', 'urgency_classifier'],
            outports=["out_"],
        )

    def run(self) -> None:
        while True:
            merged: dict = {}
            for inport in self.inports:
                msg = self.recv(inport)
                if isinstance(msg, dict):
                    merged.update(msg)
            self.send(merged, "out_")


role = AgentRoleEntry(
    name="synchronizer",
    in_ports=('summarizer', 'sentiment_classifier', 'urgency_classifier',),
    out_ports=("out",),
    factory=_Synchronizer,
    description=(
        "Wait for one message on each of 3 inports — "
        "summarizer, sentiment_classifier, urgency_classifier — dict-merge, emit on out."
    ),
)
